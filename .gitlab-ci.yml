image: nokeedev/generic-builder

build:
  stage: build
  script: gradle assemble -i
  artifacts:
    when: on_failure
    paths:
      - subprojects/docs/build/

deploy:
  stage: deploy
  script: gradle bintrayUpload
  only:
    - master
