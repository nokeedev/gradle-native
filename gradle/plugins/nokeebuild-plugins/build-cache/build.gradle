plugins {
    id 'dev.gradleplugins.java-gradle-plugin'
    id 'dev.gradleplugins.gradle-plugin-unit-test'
}

gradlePlugin {
    plugins {
        buildCache {
            id = 'nokeebuild.build-cache'
            implementationClass = 'nokeebuild.buildcache.BuildCachePlugin'
        }
    }
}
