plugins {
	id 'java-platform'
}

description = "Provides a platform that constrains versions of external dependencies used by Nokee build."

dependencies {
	constraints {
		// Gradle plugins
		api 'dev.gradleplugins:gradle-plugin-development:1.6.2'
		api 'org.shipkit:shipkit-auto-version:1.1.19'

		// Java libraries
	}
}
