plugins {
	id 'dev.gradleplugins.gradle-plugin-development' version '1.6.8'
}

rootProject.name = 'sample-kit-plugins'

include 'templates'

gradle.allprojects {
	repositories {
		mavenCentral()
		gradlePluginPortal()
	}
}
