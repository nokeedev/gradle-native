package dev.nokee.runtime.nativebase;

/**
 * Represent how a native library should be linked into a binary.
 * Use {@link TargetLinkageFactory} to create new instances.
 *
 * @since 0.5
 */
public interface TargetLinkage {
}
