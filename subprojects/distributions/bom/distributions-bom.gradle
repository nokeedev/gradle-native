plugins {
	id 'java-platform'
	id 'maven-publish'
}

def allPlatformProjects = rootProject.subprojects - [project(':docs'), project(':docs:exemplarKit'), project(':distributions'), project(':distributions:bom'), project(':distributions:all'), project, project(':internalTesting')]

dependencies {
	constraints {
		allPlatformProjects.each {
			api it
		}
	}
}

publishing {
	publications {
		nokeePlatform(MavenPublication) {
			from components.javaPlatform
			artifactId = 'nokee-platform'
		}
	}
}
