plugins {
	id 'java-library'
	id 'groovy-base'
	id 'maven-publish'
}

dependencies {
	compileOnly gradleApi(minimumGradleVersion)
}

publishing {
	publications {
		library(MavenPublication) {
			from components.java
		}
	}
}
